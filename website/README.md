# The AI Centre for Harlow CIC Website

A static landing page concept for **The AI Centre for Harlow CIC**, with interactive circular focus areas.

## Open directly

Open `index.html` from this folder in a browser.

## Run locally

From the repository root:

```bash
python3 -m http.server 4173 --directory website
```

Then visit <http://localhost:4173>.
